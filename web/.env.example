NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://localhost:27017/5wh-news
# Comma separated list of allowed CORS origins
CORS_ORIGINS=https://ec2-16-52-123-203.ca-central-1.compute.amazonaws.com:5000,http://127.0.0.1:3000

# Enable basic rate limiting (true/false)
ENABLE_RATE_LIMIT=false
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=1000

# Scheduler interval (ms) for checking scheduled content
SCHEDULER_INTERVAL_MS=60000

# JWT secret, replace in production
JWT_SECRET=replace_me_with_secure_secret

# Media URL (used by client if provided)
REACT_APP_MEDIA_URL=https://ec2-16-52-123-203.ca-central-1.compute.amazonaws.com:5000
